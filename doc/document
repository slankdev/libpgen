NOTE 開発の流れは以下に沿って行う
 
@ アドレスのフォーマット（絶対これに合わせる！！）
 * ip  アドレス		--->	u_int32_t	<stdint.h>
 * mac アドレス		--->	u_char[6]	<types.h>
 * port				--->	int			<stdio.h>


@ その他決まりごと
 * 各ファイルの最初にライセンスは載せるがrelease tagがついているバージョンのみ
 　他はLICENCEファイルに書き込んでおくだけでいい（邪魔でめんどくさいので）
